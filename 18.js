// function computingSimpleInterest(p, r, t) {
//     r = r/100;
//     let a = p * (1 + r * t);
//     return `After ${t} years at ${r}%, the investment will be worth $${a}.`;
//   }

//   let principal = Number(prompt("Enter the principal: "));
//   let rate = Number(prompt("Enter the rate of interest: "));
//   let years = Number(prompt("Enter the number of years: "));

//   console.log(computingSimpleInterest(principal, rate, years));
