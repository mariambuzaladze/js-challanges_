// function computingSimpleInterest(p, r, t, n) {
//   r = r / 100;
//   let a = p * (1 + r / n) * n * t;
//   return `$${a}`;
// }

// let principal = Number(prompt("Enter the principal: "));
// let rate = Number(prompt("Enter the rate of interest: "));
// let years = Number(prompt("Enter the number of years: "));
// let number = Number(prompt("Enter the number of interest accruals:"));

// console.log(computingSimpleInterest(principal, rate, years, number));
